## replace_many()

# Die Funktion ersetzt Werte von vorgegebenen Variablen mit dein Werten einer
# anderen Reihe

# data_old = Datensatz in dem ersetzt werden soll
# data_replacement = Datensatz aus dem ersetzt werden soll
# columns = Spalten die ersetzt werden sollen
# row = Reihe aus der die Werte zum Ersetzten genommen werden

replace_many() <- function(data_old, data_replacement, columns, row) {
  
}